<!-- reset_password_email.html.erb -->
<p>Hello <%= @user.name %>,</p>

<p>Someone has requested a link to change your password. You can do this through the link below.</p>

<p>Click the link below to reset your password:</p>
<%= link_to 'Reset Password', edit_user_password_url(reset_password_token: @token) %>
<p>Alternatively, you can copy and paste the following URL into your browser:</p>
<pre><%= edit_user_password_url(reset_password_token: @token) %></pre>
<%# <%= link_to "Reset Password", "http://localhost:8081/ResetPasswordScreen?token=#{reset_password_token}" %>
<%# <%= link_to "Reset Password", "booking-reminder-app-frontend://ResetPasswordScreen?token=#{token}" %>
<%# <%= link_to "Reset Password", "exp://192.168.0.3:8081/../ResetPasswordScreen" %>

<p>If you didn't request this, please ignore this email.</p>
<p>Your password won't change until you access the link above and create a new one.</p>
<p>Thanks,</p>